import { has, isEmpty } from "lodash";
import React, { useEffect } from "react";


export default function OfficeHourStudentPage({ token, userProfile }) {

    return (
        <>
            <h1>Office Hour Student Page</h1>
        </>
    );
}